@model IEnumerable<DatabaseReservation.Models.ReservedTable>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Reservation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Table)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Reservation.ReservationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Table.TableId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReservedTableId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReservedTableId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReservedTableId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
